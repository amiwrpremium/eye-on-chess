{
  "name": "@eyeonchess/chess",
  "version": "1.1.5",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.0",
    "typescript": "^5",
    "vitest": "^4"
  },
  "dependencies": {
    "chess.js": "^1.4.0"
  }
}
